{
  "private": true,
  "scripts": {
    "build": "nuxt build --hostname 127.0.0.1 --port 3000",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.3.1",
    "sass": "^1.59.3",
    "vue": "^3.2.47"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3",
    "nuxt-umami": "^2.0.1"
  }
}
